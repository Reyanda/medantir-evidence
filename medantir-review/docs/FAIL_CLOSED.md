# Fail-closed retrieval

Search execution is complete only when the adapter can prove that the returned records reconcile with the source-reported result set. Oversized, interrupted, repeated-page, authentication-blocked or otherwise incomplete searches fail the stage and request re-planning or human intervention.
